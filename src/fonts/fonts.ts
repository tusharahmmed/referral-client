import {Inter, Work_Sans} from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const sans = Work_Sans({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});
